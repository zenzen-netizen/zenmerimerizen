# Dependencies
node_modules/

# Environment & secrets
.env
.env.*
!.env.example
.envrypt

# User config (contains RPC URL, wallet key, API keys)
user-config.json
gmgn-config.json

# Saved config presets (full snapshots — contain API keys/identity, local-only)
presets/

# /export racikan output (bundles — local-only, transfer manually)
exports/

# Stray Claude Code worktrees
.claude/worktrees/

# Runtime state (position tracking, lessons — user-specific)
state.json
lessons.json
lessons-archive-pre-mainzen_v2.json
smart-wallets.json
pool-memory.json
candidate-memory.json
token-blacklist.json
strategy-library.json
decision-log.json
hivemind-cache.json
signal-weights.json
sol-balance-history.json

# Logs
logs/

# OS
.DS_Store

# Dev context
context.md
docs/

# Test files
test-*.js
gas-log.json
llm-cost-log.json
